export type Person = {
  label: string;
  name: string;
  nickname: string;
  picture: string;
};

export type Mini = {
  id: string;
  name: string;
  quantity: number;
  price: number;
  notes: string;
};
